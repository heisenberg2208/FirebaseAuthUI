<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devendra.firebaseauthui">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SellCrop" />
        <activity android:name=".ModeratorHome" />
        <activity android:name=".VerifyCrop" />
        <activity android:name=".Portal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Buyer" />
        <activity android:name=".BuyerList" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".FarmerList" />
        <activity android:name=".GetUserName" />
        <activity android:name=".Waiting" />
        <activity android:name=".FarmerPortal"></activity>
    </application>

</manifest>